server:
  servlet:
    context-path: /api/employee-enrollment
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/allianz_employee_db
    username: root
    password:
  #    hikari:
  #      minimum-idle: 10
  #      maximum-pool-size: 100
  #      idle-timeout: 60000
  #      connection-timeout: 30000
  #      leak-detection-threshold: 30000
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
